/**
 * URL de base de l'API backend.
 * En développement, le proxy Angular (proxy.conf.json) redirige /api
 * vers http://localhost:8081 — aucun secret ni hôte en dur dans le code métier.
 */
export const API_BASE_URL = '/api';
